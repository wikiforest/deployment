<match app.*>
    @type file
    flush_mode immediate
    path /data/app/app
    append true
    <buffer time>
        @type memory
        timekey 1d
        timekey_use_utc true
        retry_type exponential_backoff
        flush_mode immediate
    </buffer>
</match>
